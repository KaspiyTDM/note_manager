# Создаем пустой список для хранения заголовков
titles_input = []
print("Программа для добавления заголовков заметки")
print("Введите заголовки заметок. Для завершения ввода оставьте строку пустой или введите 'стоп'.\n")
# Запускаем цикл для ввода заголовков
while True:
    title = input("Введите заголовок заметки (или оставьте пустым/введите 'стоп' для завершения): ").strip()
    # Условие для выхода из цикла
    if title == "" or title.lower() == "стоп":
        print("\nВвод завершен.")
        break
    # Добавляем заголовок в список, если он не пустой
    if title:
        # Проверка на уникальность
        if title in titles_input:
            print(f"Заголовок '{title}' уже существует. Повтор не добавлен.")
        else:
            titles_input.append(title)
            print(f"Добавлен заголовок: {title}")
# Вывод списка всех добавленных заголовков
if titles_input:
    print("\nСписок всех заголовков заметки:")
    for idx, title in enumerate(titles_input, 1):
        print(f"{idx}. {title}")
else:
    print("\nСписок заголовков пуст.")
